package(default_visibility = ["//visibility:public"])

load("//:tools/bazel/macros.bzl", "resources")
load("//:tools/bazel/harness.bzl", "application_version")

resources()

filegroup(
    name = "resource",
    srcs = glob(["*"]),
)
